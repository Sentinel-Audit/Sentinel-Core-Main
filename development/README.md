# Development notes

Scheduled jobs and automation examples live under **[cron/](cron/)** — Linux crontab, systemd timers, GitHub Actions `schedule`, Node schedulers, Docker/Kubernetes, troubleshooting, and sample files in `cron/examples/`.

Copied from the internal `client-ideas/dev-tools/cron/` pack for public distribution (no secrets in samples).
